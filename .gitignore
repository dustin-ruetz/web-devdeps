# Related file: .vscode/settings.json

# Use this .gitignore file as the `--ignore-path` for ESLint and Prettier as well,
# since everything that's excluded from repo source control shouldn't be checked.

# Ignore the `_/` underscore directory since it's only used to develop and test the `init-repo` script locally.
_/
.caches/
coverage/
lib/
node_modules/
# Related file: src/eslint.config.cts (specifically `eslintConfig.ignorePatterns`)
!*.*.js
!*.*.ts
# Ignore the changelog file since:
# 1. It's auto-generated; and
# 2. To prevent Prettier from checking its formatting and then warning about code style issues.
CHANGELOG.md
